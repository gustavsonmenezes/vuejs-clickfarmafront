    # Porta do servidor
    server.port=8080

    # /api/receita/processar pode levar >30s (OCR + Tesseract)
    spring.mvc.async.request-timeout=2m

    # Banco de Dados MySQL (usando o nome do serviço 'db' do docker-compose)
    spring.datasource.url=jdbc:mysql://db:3306/clickfarma?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    spring.datasource.username=root
    spring.datasource.password=guga1010@
    spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

    # JPA Configuration
    spring.jpa.hibernate.ddl-auto=update
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
    spring.jpa.properties.hibernate.format_sql=true

    # JWT Configuration
    jwt.secret=clickfarmaSecretKey2026VeryLongSecretKeyForJWTGeneration
    jwt.expiration=86400000
